/**
 * PRIME HOUSE — cart page entry. Compiles to public/checkout.js
 * (Theme Raed's own webpack config maps the `cart` entry to a
 * `checkout.js` output file — mirrored here since pages/cart.twig
 * references {{ 'checkout.js' | asset }}. Configure the same output
 * naming in your webpack entry once you merge this into the official
 * `salla theme create` scaffold — see README.md).
 * No custom cart logic: <salla-cart-summary-card>, <salla-cart-coupons>,
 * <salla-quantity-input> handle all cart behaviour natively.
 */
